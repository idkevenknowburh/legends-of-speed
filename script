-- ============================================
-- Legends of Speed | Sheitsa Hub V2
-- UI: Rayfield by Sirius
-- ============================================

-- ========== SERVICES ==========
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local RS = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer
local DiscordLink = "https://discord.gg/ZE6vyxgydK"

-- ========== THREAD MANAGEMENT ==========
local farmThreads = {}

local function stopFarm(name)
    farmThreads[name] = false
end

-- ========== RAYFIELD UI ==========
local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Window = Rayfield:CreateWindow({
    Name = "Legends of Speed | Sheitsa Hub V2",
    Icon = 0,
    LoadingTitle = "Legends of Speed",
    LoadingSubtitle = "by Sheitsa",
    Theme = "Light",
    ToggleUIKeybind = Enum.KeyCode.RightControl,
    ConfigurationSaving = {Enabled = false},
    DisableRayfieldPrompts = true,
})

local Tab = {
    Home      = Window:CreateTab({Name = "Home",      Icon = "home"}),
    Pets      = Window:CreateTab({Name = "Pets",      Icon = "sparkles"}),
    Teleports = Window:CreateTab({Name = "Teleports", Icon = "map-pin"}),
    Credits   = Window:CreateTab({Name = "Credits",   Icon = "heart"}),
}

-- ==========================================
--  HOME TAB (Auto-Farm)
-- ==========================================
Tab.Home:CreateSection({Name = "Auto-Farm"})

Tab.Home:CreateToggle({
    Name = "Auto Orbs",
    CurrentValue = false,
    Callback = function(on)
        stopFarm("orbs")
        if on then
            farmThreads["orbs"] = true
            spawn(function()
                while farmThreads["orbs"] == true do
                    task.wait()
                    if farmThreads["orbs"] == true then
                        local char = player.Character
                        if char and char:FindFirstChild("HumanoidRootPart") then
                            local hrp = char.HumanoidRootPart
                            local orbFolder = workspace:FindFirstChild("orbFolder")
                            if orbFolder then
                                local city = orbFolder:FindFirstChild("City")
                                if city then
                                    for _, v in pairs(city:GetChildren()) do
                                        if farmThreads["orbs"] == true and v.Name ~= "Gem" and v:FindFirstChild("outerOrb") then
                                            v.outerOrb.CFrame = hrp.CFrame
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end,
})

Tab.Home:CreateToggle({
    Name = "Auto Steps",
    CurrentValue = false,
    Callback = function(on)
        stopFarm("steps")
        if on then
            farmThreads["steps"] = true
            spawn(function()
                while farmThreads["steps"] == true do
                    task.wait(0.5)
                    if farmThreads["steps"] == true then
                        local rEvents = RS:FindFirstChild("rEvents")
                        if rEvents then
                            local ev = rEvents:FindFirstChild("orbEvent")
                            if ev then ev:FireServer("collectOrb", "Orange Orb", "City") end
                        end
                    end
                end
            end)
        end
    end,
})

Tab.Home:CreateToggle({
    Name = "Auto Rebirths",
    CurrentValue = false,
    Callback = function(on)
        stopFarm("rebirths")
        if on then
            farmThreads["rebirths"] = true
            spawn(function()
                while farmThreads["rebirths"] == true do
                    task.wait(5)
                    if farmThreads["rebirths"] == true then
                        local rEvents = RS:FindFirstChild("rEvents")
                        if rEvents then
                            local ev = rEvents:FindFirstChild("rebirthEvent")
                            if ev then ev:FireServer("rebirthRequest") end
                        end
                    end
                end
            end)
        end
    end,
})

Tab.Home:CreateToggle({
    Name = "Auto Gems",
    CurrentValue = false,
    Callback = function(on)
        stopFarm("gems")
        if on then
            farmThreads["gems"] = true
            spawn(function()
                while farmThreads["gems"] == true do
                    task.wait()
                    if farmThreads["gems"] == true then
                        local char = player.Character
                        if char and char:FindFirstChild("HumanoidRootPart") then
                            local hrp = char.HumanoidRootPart
                            local orbFolder = workspace:FindFirstChild("orbFolder")
                            if orbFolder then
                                local city = orbFolder:FindFirstChild("City")
                                if city then
                                    for _, v in pairs(city:GetChildren()) do
                                        if farmThreads["gems"] == true and v.Name == "Gem" and v:FindFirstChild("outerGem") then
                                            v.outerGem.CFrame = hrp.CFrame
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end,
})

Tab.Home:CreateToggle({
    Name = "Auto Hoops",
    CurrentValue = false,
    Callback = function(on)
        stopFarm("hoops")
        if on then
            farmThreads["hoops"] = true
            spawn(function()
                while farmThreads["hoops"] == true do
                    task.wait()
                    if farmThreads["hoops"] == true then
                        local char = player.Character
                        if char and char:FindFirstChild("HumanoidRootPart") then
                            local hrp = char.HumanoidRootPart
                            local hoops = workspace:FindFirstChild("Hoops")
                            if hoops then
                                for _, v in pairs(hoops:GetChildren()) do
                                    if farmThreads["hoops"] == true then
                                        v.CFrame = hrp.CFrame
                                    end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end,
})

-- ==========================================
--  PETS TAB
-- ==========================================
Tab.Pets:CreateSection({Name = "OP Pets"})

Tab.Pets:CreateToggle({
    Name = "Inf Glitch Pet",
    CurrentValue = false,
    Callback = function(on)
        stopFarm("glitchPet")
        if on then
            farmThreads["glitchPet"] = true

            -- Thread 1: Rebirth spam
            spawn(function()
                while farmThreads["glitchPet"] == true do
                    task.wait()
                    if farmThreads["glitchPet"] == true then
                        local rEvents = RS:FindFirstChild("rEvents")
                        if rEvents then
                            local ev = rEvents:FindFirstChild("rebirthEvent")
                            if ev then ev:FireServer("rebirthRequest") end
                        end
                    end
                end
            end)

            -- Thread 2: Yellow orb spam (40 fires every 0.1s)
            spawn(function()
                while farmThreads["glitchPet"] == true do
                    task.wait(0.1)
                    if farmThreads["glitchPet"] == true then
                        local rEvents = RS:FindFirstChild("rEvents")
                        if rEvents then
                            local ev = rEvents:FindFirstChild("orbEvent")
                            if ev then
                                for _ = 1, 40 do
                                    if farmThreads["glitchPet"] == true then
                                        ev:FireServer("collectOrb", "Yellow Orb", "City")
                                    end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end,
})

Tab.Pets:CreateButton({
    Name = "Unlock Swift Samurai",
    Callback = function()
        local petShop = RS:FindFirstChild("cPetShopFolder")
        local petShopRemote = RS:FindFirstChild("cPetShopRemote")
        if petShop and petShopRemote then
            local swiftSamurai = petShop:FindFirstChild("Swift Samurai")
            if swiftSamurai then
                petShopRemote:InvokeServer(swiftSamurai)
            end
        end
    end,
})

-- ==========================================
--  TELEPORTS TAB
-- ==========================================
Tab.Teleports:CreateSection({Name = "Islands"})

Tab.Teleports:CreateButton({
    Name = "Main City",
    Callback = function()
        local char = player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = CFrame.new(-576.270142, 4.1662488, 410.038574, -0.998445749, -5.50801449e-09, 0.0557322279, -4.12241367e-13, 1, 9.88225892e-08, -0.0557322279, 9.86689699e-08, -0.998445749)
        end
    end,
})

Tab.Teleports:CreateButton({
    Name = "Snow City",
    Callback = function()
        local char = player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = CFrame.new(-488.590027, 4.1662488, 2301.04224, -0.499920726, 1.05356229e-07, -0.866071165, -2.12529863e-12, 1, 1.21649691e-07, 0.866071165, 6.08170438e-08, -0.499920726)
        end
    end,
})

Tab.Teleports:CreateButton({
    Name = "Magma City",
    Callback = function()
        local char = player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = CFrame.new(2345.34839, 4.1662488, 4393.5, 0.228262901, 1.14109731e-07, 0.973599553, -6.69713807e-12, 1, -1.17202404e-07, -0.973599553, 2.67464397e-08, 0.228262901)
        end
    end,
})

Tab.Teleports:CreateButton({
    Name = "Legends Speed Highway",
    Callback = function()
        local char = player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = CFrame.new(3709.70703, 71.7461929, 5593.875, -0.0438015647, -1.0088355e-08, -0.999040246, -2.20692842e-14, 1, -1.00980451e-08, 0.999040246, -4.42288151e-10, -0.0438015647)
        end
    end,
})

Tab.Teleports:CreateButton({
    Name = "Jungle Island City",
    Callback = function()
        local char = player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = CFrame.new(1681.85168, 3.14525414, 12818.3633, 0.456854314, 6.8587525e-08, -0.889541507, -2.31062192e-08, 1, 6.52373698e-08, 0.889541507, -9.25003008e-09, 0.456854314)
        end
    end,
})

-- ==========================================
--  CREDITS TAB
-- ==========================================
Tab.Credits:CreateParagraph({
    Title = "Legends of Speed",
    Content = "Sheitsa Hub V2\nScript by Sheitsa Dev Team.",
})

Tab.Credits:CreateParagraph({
    Title = "UI Library",
    Content = "Rayfield by Sirius\nhttps://sirius.menu",
})

Tab.Credits:CreateSection({Name = "Social"})

Tab.Credits:CreateButton({
    Name = "Join our Discord",
    Callback = function()
        pcall(setclipboard, DiscordLink)
        Rayfield:Notify({Title = "LoS", Content = "Discord link copied!", Duration = 3})
    end,
})

-- ==========================================
--  CHARACTER DEATH CLEANUP
-- ==========================================
player.CharacterAdded:Connect(function()
    for _, n in ipairs({"orbs", "steps", "gems", "hoops", "rebirths", "glitchPet"}) do
        stopFarm(n)
    end
end)

-- ==========================================
--  STARTUP NOTIFICATION
-- ==========================================
Rayfield:Notify({
    Title = "Legends of Speed | Sheitsa Hub V2",
    Content = "Script loaded! Press RightControl to toggle UI.",
    Duration = 4,
})

-- ==========================================
--  DRAGGABLE TOGGLE BUTTON
-- ==========================================
local toggleGui = Instance.new("ScreenGui")
toggleGui.Name = "LoSSheitsaToggle"
toggleGui.ResetOnSpawn = false
toggleGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
toggleGui.Parent = CoreGui

local hubVisible = true

local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 44, 0, 44)
toggleBtn.Position = UDim2.new(0.5, -22, 0, 14)
toggleBtn.BackgroundColor3 = Color3.fromRGB(12, 12, 16)
toggleBtn.Text = ""
toggleBtn.BorderSizePixel = 0
toggleBtn.AutoButtonColor = false
toggleBtn.Parent = toggleGui
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0, 11)

local tStroke = Instance.new("UIStroke", toggleBtn)
tStroke.Color = Color3.fromRGB(190, 190, 200)
tStroke.Thickness = 1.5

local tLabel = Instance.new("TextLabel", toggleBtn)
tLabel.Size = UDim2.new(1, 0, 1, 0)
tLabel.Text = "LoS"
tLabel.TextSize = 12
tLabel.BackgroundTransparency = 1
tLabel.TextColor3 = Color3.fromRGB(200, 200, 210)
tLabel.Font = Enum.Font.GothamBold

-- Drag + Click (with 5px drag threshold)
local htDrag, htDragStart, htStartPos, htDidDrag
toggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        htDrag = true; htDidDrag = false
        htDragStart = input.Position; htStartPos = toggleBtn.Position
    end
end)
toggleBtn.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        if not htDidDrag then
            hubVisible = not hubVisible
            Rayfield:SetVisibility(hubVisible)
            tLabel.Text = hubVisible and "LoS" or "--"
            tStroke.Color = hubVisible and Color3.fromRGB(190, 190, 200) or Color3.fromRGB(80, 80, 90)
            toggleBtn.BackgroundColor3 = hubVisible and Color3.fromRGB(12, 12, 16) or Color3.fromRGB(30, 30, 36)
        end
        htDrag = false
    end
end)
UIS.InputChanged:Connect(function(input)
    if htDrag and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local d = input.Position - htDragStart
        if math.abs(d.X) > 5 or math.abs(d.Y) > 5 then htDidDrag = true end
        toggleBtn.Position = UDim2.new(htStartPos.X.Scale, htStartPos.X.Offset + d.X, htStartPos.Y.Scale, htStartPos.Y.Offset + d.Y)
    end
end)

warn("[LoS Sheitsa Hub V2] >> Loaded with Rayfield UI!")
